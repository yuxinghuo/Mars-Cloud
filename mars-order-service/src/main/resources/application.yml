server:
  port: 9000
spring:
  application:
    name: mars-order-service

  cloud:
    nacos:
      discovery:
        # 指定nacos server地址
        server-addr: localhost:8848
        # 指定集群名称
        cluster-name: NJ
        metadata:
          version: v1

